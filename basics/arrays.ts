const superHeroes: string[] = [];
// const heroPower: number[] = [];
const heroPower: Array<number> = [];

superHeroes.push("spidermand");
heroPower.push(2);

//
//

type User = {
    name: string;
    isActive: boolean;
};

const allUsers: User[] = [];

allUsers.push({
    name: "henry",
    isActive: true,
});

//
//

const arrayInsideArray: number[][] = [
    [255, 255, 255],
    [1, 1, 1],
];

export {};
